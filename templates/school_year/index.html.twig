{% extends 'base.html.twig' %}

{% block title %}Années scolaire
{% endblock %}

{% block body %}
	<div
		class="container mx-auto px-6 py-8">
		{# Header with title and button #}
		<div class="flex justify-between items-center mb-6">
			<h1 class="text-3xl font-bold text-gray-900">Années scolaire</h1>
			<twig:Button label="Ajouter une année" variant="blue" type="button"/>
		</div>

		{# Description text #}
		<div class="mb-8">
			<p class="text-gray-700 mb-2">
				Les années scolaires permettent de garder en mémoire les interventions réalisées sur les années antérieures.
			</p>
			<p class="text-gray-700">
				La dernière période de cours sur l'année permet de déterminer la fin de l'année. Lorsque cette dernière période de cours est passée, le programme passe automatiquement à l'année suivante (prochaine saison).
			</p>
		</div>

		{# Table #}
		<div class="bg-white rounded-lg shadow overflow-hidden">
			<table class="min-w-full divide-y divide-gray-200">
				<thead class="bg-gray-50">
					<tr>
						<th scope="col" class="px-6 py-3 text-left text-sm font-medium text-gray-700">
							Année promotion
						</th>
						<th scope="col" class="px-6 py-3 text-left text-sm font-medium text-gray-700">
							Saison
						</th>
						<th scope="col" class="px-6 py-3"></th>
					</tr>
				</thead>
				<tbody class="bg-white divide-y divide-gray-200">
					{% for schoolYear in schoolYears %}
						<tr class="hover:bg-gray-50">
							<td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
								{{ schoolYear.year }}
							</td>
							<td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
								{{ schoolYear.saison }}
							</td>
							<td class="px-6 py-4 whitespace-nowrap text-right text-sm">
								<a href="#" class="text-gray-600 hover:text-gray-900 inline-flex items-center">
									<svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
										<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
										<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/>
									</svg>
									Accéder à la fiche
								</a>
							</td>
						</tr>
					{% endfor %}
				</tbody>
			</table>
		</div>
	</div>
{% endblock %}
